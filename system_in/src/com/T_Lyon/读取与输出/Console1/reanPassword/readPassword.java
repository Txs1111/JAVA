package com.T_Lyon.读取与输出.Console1.reanPassword;

/**
 * @author Lyon
 * 2021-5-22 16:17:48
 * 测试Console包下的readPassword
 */
public class readPassword {
    /**
     * @Param prompt 显示字符创prompt（提示符），并读取用户输入，直到输入行结束。
     * @Param args参数可以用来提供格式参数
     */
    static char[] readPassword(String prompt, Object... ages) {
        return new char[0];
    }
}
